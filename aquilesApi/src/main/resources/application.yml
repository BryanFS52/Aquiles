server:
  port: 8080
# Cambia 8081 por el puerto que prefieras
spring:
  application:
    name: aquilesApi

  datasource:
    url: jdbc:postgresql://localhost:5432/DB_Aquiles
    username: postgres
    password: "2104"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: appaquiles@gmail.com
    password: cjoudiukabtkazxp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  main:
    allow-circular-references: true
    allow-bean-definition-overriding: true

  scheduling:
    enabled: true

  firebase:
    config:
      file: config/firebase-config.json

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  graphql:
    path: aquiles/graphql
    graphiql:
      enabled: true
      path: /graphiql
    cors:
      allowed-origin-patterns: "*"
      allowed-methods: GET,POST,PUT,DELETE,OPTIONS
      allow-credentials: true
      max-age: 3600

frontend:
  url: https://7d12-179-1-218-161.ngrok-free.app
