server:
  port: 8081  # Cambia 8081 por el puerto que prefieras
spring:
  application:
    name: DBAquiles

  datasource:
    url: jdbc:postgresql://localhost:5432/DB_Aquiles
    username: postgres
    password: "Nathaly1510*"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      #ddl-auto: create-drop
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: appaquiles@gmail.com
    password: cjoudiukabtkazxp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  main:
    allow-circular-references: true
    allow-bean-definition-overriding: true

  scheduling:
    enabled: true

  firebase:
    config:
      file: config/firebase-config.json

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB