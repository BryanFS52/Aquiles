export interface AssignmentCard {
    id: string;
    title: string;
    description: string;
    route: string;
    buttonText: string;
}

export interface FichasCoordinadorProps {
    isDarkMode?: boolean;
}
